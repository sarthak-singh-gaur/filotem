import sharp from 'sharp'; import fs from 'fs'; import path from 'path'; const svgBuffer = fs.readFileSync('public/favicon.svg'); const sizes = {'mipmap-mdpi': 48, 'mipmap-hdpi': 72, 'mipmap-xhdpi': 96, 'mipmap-xxhdpi': 144, 'mipmap-xxxhdpi': 192}; const basePath = 'android/app/src/main/res/'; Object.keys(sizes).forEach(async (folder) => { const size = sizes[folder]; const fullPath = path.join(basePath, folder); if (!fs.existsSync(fullPath)) fs.mkdirSync(fullPath, {recursive: true}); await sharp(svgBuffer).resize(size, size).png().toFile(path.join(fullPath, 'ic_launcher.png')); await sharp(svgBuffer).resize(size, size).png().toFile(path.join(fullPath, 'ic_launcher_round.png')); await sharp(svgBuffer).resize(size, size).png().toFile(path.join(fullPath, 'ic_launcher_foreground.png')); console.log('Generated ' + folder); });
